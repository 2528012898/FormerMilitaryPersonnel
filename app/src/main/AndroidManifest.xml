<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.qiye.formermilitaryp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".application.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.StartPageActivity"
            android:theme="@style/welcomeTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.RegActivity" />
        <activity
            android:name=".activity.person.SexSelectActivity"
            android:theme="@style/Theme.Transparent" />
        <activity android:name=".activity.person.KunNanBangFuShowOneActivity" />
        <activity android:name=".activity.person.KunNanBangFuListActivity" />
        <activity android:name=".activity.person.KunNanBangFuAddActivity" />
        <activity android:name=".activity.person.EmploymentListActivity"></activity>
    </application>

</manifest>